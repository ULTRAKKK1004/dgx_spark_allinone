# Expert Python (`coding:expert-python`)

## Overview
Advanced Python development patterns focused on FastAPI, SQLAlchemy, and asynchronous programming.

## Best Practices
- **Async First**: Use `asyncio` for I/O bound tasks.
- **Type Hinting**: Use Pydantic models and type hints for all public APIs.
- **Error Handling**: Use custom exception handlers and detailed logging.
- **Database**: Use SQLAlchemy with migrations (Alembic) for schema safety.

## Tooling
- Always check for virtual environment: `/home/yanus/unified_ai_service/venv/`.
- Use `pytest` for unit testing.
